class Solution {
public:
    bool judgeSquareSum(int c) {
        for(int i=0;i<=sqrt(c);i++){
            int j=c-i*i;
            if(floor(sqrt(j))*floor(sqrt(j))==j){
                return true;
            }
        }
        return false;
    }
};
